spring:
  config:
    activate:
      on-cloud-platform: kubernetes
  cloud:
    kubernetes:
      loadbalancer:
        mode: service

eureka:
  client:
    fetch-registry: false
    register-with-eureka: false
    enabled: false

server:
  port: 8080